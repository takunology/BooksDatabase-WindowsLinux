@page
@model PersonalDataModel
@{
    ViewData["Title"] = "個人情報";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>アカウントの個人情報についてのデータをダウンロードまたは削除できます。</p>
        <p>
            <strong>削除したデータは復旧できないので注意してください。</strong>
        </p>
        <form asp-page="DownloadPersonalData" method="post" class="form-group">
            <button class="btn btn-default" type="submit">ダウンロード</button>
        </form>
        <p>
            <a asp-page="DeletePersonalData" class="btn btn-default">削除</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}